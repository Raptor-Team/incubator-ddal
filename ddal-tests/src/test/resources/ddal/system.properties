#SequenceServer相关配置
sequence.zk.address=127.0.0.1:2181
sequence.zk.namespace=crm

##执行线程池参数设置
#核心线程数，默认为（1*CPU核数 + 1），此值不能超过最大线程数
executor.coreThreadSize=20
#最大线程数，默认为（2*CPU核数 + 1）
executor.maxThreadSize=50
#线程等待队列大小，默认为100
executor.queueSize=500